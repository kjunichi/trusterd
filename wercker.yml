box: wercker-labs/docker
build:
    steps:
        - script:
            name: docker version
            code: |
              uname -a
              cat /etc/issues
              docker -v
        - script:
            name: docker build
            code: |
              docker build -t trusterd_ci_1 .
